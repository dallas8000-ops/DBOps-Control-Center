# Stripe + Render setup (deprecated)

**Production is on Railway.** Use [`STRIPE_RAILWAY_SETUP.md`](./STRIPE_RAILWAY_SETUP.md) instead.

This file is kept for buyers who still deploy via `render.yaml`. Render is no longer the seller's live demo host.
